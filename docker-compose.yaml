version: '3.1'
services:
  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: example
    volumes:
      - ./db_data:/var/lib/mysql
    ports:
      - 3306:3306

  app:
    build: .
    volumes:
      - .:/app
    depends_on:
      - db
